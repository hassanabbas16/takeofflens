# ---- Base ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
python-multipart==0.0.20

# ---- CV / OCR ----
# Pinned to an exact pair. PaddleOCR 2.x and 3.x have incompatible APIs
# (2.x: ocr.ocr(img, cls=True) -> nested lists; 3.x: ocr.predict(img) -> result dicts
# with rec_texts/rec_scores/rec_polys). We target 3.x and pin both halves so an
# upstream minor release cannot silently switch the API under us.
# paddleocr 3.4.1 pulls paddlex 3.4.x, which is the piece that actually resolves models.
paddlepaddle==3.1.1
paddleocr==3.4.1
opencv-python-headless==4.10.0.84
pymupdf==1.25.1
numpy==1.26.4

# ---- Phase 3 (LLM) ----
# Anthropic SDK 1.x. messages.parse(output_format=Model) is the structured-outputs entry
# point and returns a validated instance on response.parsed_output.
anthropic==1.6.0
pyyaml==6.0.2

# ---- Dev ----
pytest==8.3.4
httpx==0.28.1
ruff==0.8.6
